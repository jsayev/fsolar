<div class="container-fluid">
    <div class="row px-2">
        <div class="col-12 mt-3">
            <ul class="nav nav-pills">
                <span class="navbar-brand">
                    F-Solar
                </span>
                {{#each navLinkOptions}}
                {{#with this}}
                <li class="nav-item mr-1">
                    <a class="nav-link {{active}} px-1" href="{{href}}">{{label}}</a>
                </li>
                {{/with}}
                {{/each}}
                <li class="nav-item dropdown ml-auto">
                    <a class="nav-link dropdown-toggle {{active}}" data-toggle="dropdown" href="#"><span
                            class="mr-1">{{username}}</span></a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="/dashboard/password" role="tab">Change password</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="/logout">Logout</a>
                    </div>
                </li>
            </ul>
        </div>
    </div>
    <hr>
</div>